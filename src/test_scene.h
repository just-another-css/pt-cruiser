#ifndef TEST_SCENE_H
#define TEST_SCENE_H

#include "include/mesh.h"

extern void initialise_test_scene_1(int* num_objects, PointsMesh** meshes, float3** lightings);
extern void initialise_test_scene_2(int* num_objects, PointsMesh** meshes, float3** lightings);
extern void initialise_test_scene_3(int* num_objects, PointsMesh** meshes, float3** lightings);
extern void initialise_test_scene_4(int* num_objects, PointsMesh** meshes, float3** lightings);
extern void initialise_test_scene_5(int* num_objects, PointsMesh** meshes, float3** lightings);

#endif
